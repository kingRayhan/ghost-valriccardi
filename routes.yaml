routes:

collections:
  /:
    permalink: /{slug}/
    template: home
    filter: featured:true

  /articles/:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
